source 'https://rubygems.org'

gem 'jekyll', '4.2.1'

gem 'webrick'

gem 'csv' # Remove this when it gets added to Jekyll runtime

gem 'base64' # Also remove this when it gets added to Jekyll runtime

gem 'logger'

gem 'bigdecimal'

